# This pod will be used to manage the artifacts in the mounted location.
apiVersion: v1
kind: Pod
metadata:
  name: tm-log-monitoring-pod
  namespace: wso2
spec:
  containers:
    - name: ubuntu-container
      image: ubuntu:latest
      command: ["/bin/bash", "-c", "chmod -R 777 /home && while true; do sleep 3600; done"]
      volumeMounts:
        - name: apim-tm-log-storage-volume
          mountPath: "/home"
  volumes:
    - name: apim-tm-log-storage-volume
      persistentVolumeClaim:
        claimName: apim-tm-log-storage-claim
