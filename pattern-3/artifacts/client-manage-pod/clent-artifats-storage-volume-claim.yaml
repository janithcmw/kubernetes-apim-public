apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clent-artifats-storage-volume-claim
  namespace: wso2
  labels:
    app:  client
    tier: runClients
spec:
  storageClassName: "azurefile-csi-premium"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi